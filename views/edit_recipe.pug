extends layout

block content
  .container
    h1 #{title}
    form(method='post', action='/recipe/edit/'+recipe._id)
      .form-group
        label Title:
        input.form-control(name='title', type='text', value=recipe.title)
      .form-group
        label ingredients:
        alert("Hello")
        //each ingredient in recipe.ingredient
        //  input.form-control(name='ingredient[]', type='text', value=ingredient)


      input.btn.btn-primary(type='submit', name='submit', value='Save')
      a.btn.btn-secondary(href='/manage/categories') Back
      a.btn.btn-danger.category-delete.pull-right(data-cat-id=category._id) Delete
